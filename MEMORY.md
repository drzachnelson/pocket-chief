# Pocket Chief Memory

## Contacts

- Zachery Nelson, MD — product owner and sole intended user; `zachpnw@gmail.com`; GitHub `drzachnelson`.

## Key Decisions

- Pocket Chief is a private, single-user PWA, not a public reference site.
- Authentication uses passwordless email OTP and an allowlisted owner email.
- The primary experience is search and browse; chat and built-in spaced repetition are out of v1.
- Topics follow an editable SCORE hierarchy and can carry personal tags.
- AI composes flexible pages from controlled content blocks. Human approval is required before indexing.
- The visual system is Clinical Atlas: cool neutrals, one cobalt accent, compact dividers, Geist typography, and restrained motion.
- Anki exports use a reviewed cloze card with the relevant section, nearest diagram, and a Pocket Chief deep link.
- Pocket Chief never accepts PHI or patient-specific information.
- V1 is implemented and locally verified. External launch remains pending Supabase/OpenAI credentials, GitHub authentication, and Vercel provisioning; four additional source packets are still needed for the five-topic launch library.
- 2026-08-13 multi-agent review (Fable-orchestrated): fixed an AnkiDialog race that silently discarded reviewed cloze edits (root cause of the flaky e2e test), enforced references-block source-ID grounding in both TS and SQL, restored case-insensitive PHI name detection with stopword guards plus a patient-narrative heuristic, and applied WCAG/craft design polish within Clinical Atlas (focus ring, light-mode faint-text contrast, 44px touch targets, mobile overflow, generic 404). Verified: vitest 72/72, playwright 12/12. Committed as 7871e90 + 9b95193 on feat/pocket-chief-v1.
- Migration rule going forward: never edit an applied migration file; 202608120001 is immutable, all schema changes land as new files (202608130001 is the catch-up for the earlier in-place edit).
- Known accepted risks (documented, not fixed): factualUnits (TS) and topic_block_expected_claims (SQL) are dual-maintained and must change together; in-memory rate limiter is single-instance only; owner_allowlist is not schema-limited to one row.
- 2026-08-13 launch packet 2 of 5: "Fibroadenoma vs Phyllodes Tumor" seeded under a new SCORE › Breast › Benign Breast Disease taxonomy branch. Kept as one comparison topic rather than two, because the packet's value is the side-by-side matrix and the "rapidly growing fibroadenoma = suspect phyllodes" link. Demo seed only — the Supabase path still seeds a single sha256-pinned launch topic and needs a new migration before this topic reaches a hosted install.
- Adding the second topic exposed three single-topic assumptions, now fixed: the topic breadcrumb and the /topics section heading were hardcoded to "Biliary Tract", the curriculum tree derived depth from array index, and search gave a full prefix score when a one-letter word in body text matched the start of a query token (so "acute wound" returned an unrelated topic). Prefix credit now requires a word of 3+ characters.
- 2026-08-13 General Abdomen batch (6 topics): Abdominal Pain, Rectus Sheath Hematoma, Desmoids & Fibromatoses, Peritoneal Neoplasms, Abdominal Exploration, PD Catheter Insertion — under a new SCORE › General Abdomen branch split into Diseases & Conditions and Operations & Procedures. Library is now 8 topics across 4 sections.
- Editorial precedent set with that batch: where board orthodoxy and current practice diverge, the topic carries both — the keyed answer plus a short "Board answer versus current practice" prose block. Desmoids is the worked example (WLE + sulindac/tamoxifen for the exam; surveillance-first and nirogacestat in practice).
- Content moved out of `src/lib/seed.ts` into `src/content/` — one file per topic, plus `sourced()` in `src/content/authoring.ts`, which derives every claim from `factualUnits()` so claim text can no longer drift from rendered text. `src/lib/seed.ts` is now a re-export so existing imports and the Supabase path are untouched. Choledocholithiasis was moved verbatim and must stay that way: its blocks are sha256-pinned by `ensure_launch_topic`, and `content-contract.test.ts` now asserts the digest against the migration.
- The flow block renderer was generalized from a hardcoded root-plus-two-branches to a level walk, so decision trees of any width and depth render. Existing flows re-verified.
- Repeatable workflow for the rest of the curriculum: `/score-topic` skill at `.claude/skills/score-topic/`, backed by `content-contract.test.ts` (claim parity, React key uniqueness, flow graph validity, citation resolution, taxonomy/category agreement, launch-topic digest) and a PostToolUse hook that runs it on any edit under `Pocket Chief/src/content/`.
- Each batch of topics has exposed latent single-library assumptions rather than content bugs: two search-scoring flaws (a one-letter body word could carry a query token; edit-distance tolerance of 2 was loose enough that "wound" matched "count") and hardcoded single-topic UI strings. Search now uses Damerau-Levenshtein with tolerance 1 for tokens under 11 characters. Expect the next batch to surface more of the same — fix the app, not the test.
- 2026-08-14: the SCORE curriculum topic outline was supplied and now lives at `Pocket Chief Resources/score-module-outline.md`. It is the source of truth for taxonomy placement — find the topic in it before inventing a branch, and ask when placement is ambiguous. It also records that the early Hernia section was split finer than SCORE splits it (Inguinal and Femoral are one SCORE entry, Groin Hernia in Children actually belongs to Pediatric Surgery, Abdominal Wall Reconstruction maps to Ventral Hernia Repair). Those splits stay as they are; new work follows the outline's titles and granularity so coverage is countable.
- 2026-08-14: real reference corpora replaced the era of hand-pasted packets. `Pocket Chief Resources/score-modules/` holds 433 SCORE module texts with their surgicalcore.org URLs and retrieval dates; `Pocket Chief Resources/absite-8e/` holds Fiser 8th edition by chapter. Both were copied into the vault rather than referenced in place, because the originals sat on the Desktop and in a dated Codex scratch folder. Sources are now registered with real citations instead of the generic `user_notes` blob used for the first four packets.
- 2026-08-14 Arterial Disease branch opened with Abdominal & Aortoiliac Aneurysm Repair, allocated from the `…0006xx` block. Library is 17 topics across 5 sections.
- `buildTopic` gained an optional `additionalSourceIds` because `TopicInput.sourceId` was singular — an assumption from when each topic had exactly one packet. The aneurysm topic cites the SCORE module, Fiser ch27, and the USPSTF recommendation, and `supportWarnings` rejects any citation missing from the version's `sourceIds`. Additive, so none of the existing 16 topic files changed.
- Editorial precedent extended: the "Board answer versus current practice" block now also covers a source being simply wrong, not just dated. Fiser's AAA surveillance table gives a three-year interval for aneurysms over 5.0 cm, which reads as a transposition of the interval belonging to the 3.0–3.9 cm band; the topic follows the SVS intervals and says why. Screening is split the same way, with the SVS recommendation and the narrower USPSTF grades as separate blocks, because they diverge specifically for women.
- Next 16 refuses a second `next dev` on the same directory whatever the port, so a dev server left running in another chat blocks browser verification outright — and it keeps serving the old `globalThis`-cached library, which is indistinguishable from new content failing to register. `curl -s localhost:3210/api/library` settles it. `.claude/launch.json` now sets `autoPort` on the pocket-chief entry, which helps with port collisions but not with this lock.
- Environment: the vault lives in iCloud-synced Documents — iCloud can evict node_modules/.next file contents (commands hang at 0% CPU) and create "name 2.ext" conflict copies inside .next that break tsc. Remedy: reinstall node_modules fresh / delete conflict copies. pnpm is not on PATH; use ./node_modules/.bin/* or `CI=true npx pnpm@11`.
- 2026-08-14: published to the private GitHub repo `drzachnelson/pocket-chief`; the working branch `feat/pocket-chief-v1` was renamed `main` and pushed. The licensed corpora are gitignored — `Pocket Chief Resources/*` with a negation for `score-module-outline.md` — so the SCORE module texts and Fiser chapters stay on this machine only. History was scanned before publishing; no `.env` file was ever committed.
- Deploying exposed the gap the demo repository had been hiding: `ensure_launch_topic` seeds exactly one sha256-pinned topic, so a hosted install showed 1 of 17. `installLibrary` (`src/lib/library-install.ts`, driven by `POST /api/library/install`) closes it by replaying each authored topic through `createTopicDraft` → `approveDraft`, the same public path a hand-authored topic takes. No new SQL and no service-role key, so RLS and `topic_block_expected_claims` still adjudicate.
- The TS and SQL claim extractors were compared unit by unit before writing that route, and they agree on every block type including multi-level flows — the SQL `flow` branch only walks `nodes` then `edges` and never encoded depth. The feared dual-maintenance break was not real. The actual mismatch was ID shape: authored taxonomy ids are readable strings (`"biliary"`) while `taxonomy_nodes.id` is a uuid, and `ensure_default_taxonomy` had already claimed `score`/`alimentary-tract`/`biliary-tract` under server-assigned uuids. Taxonomy is therefore reconciled by **slug**, which is the key `ensure_launch_topic` itself resolves by.
- `.claude/launch.json` had an absolute `/Users/zach/...` path on the pocket-chief entry, which killed the preview with `MODULE_NOT_FOUND` on this machine. Now vault-relative like the other entries. Watch for this if the preview dies immediately on a different machine.
- 2026-08-15 Supabase CLI wired up locally. `supabase init` was run in this directory (CLI 2.114.0 via Homebrew), producing `supabase/config.toml` and `supabase/.gitignore`; the two existing migrations were untouched. **Both `enable_signup` flags in config.toml were flipped to `false`** — `[auth]` and `[auth.email]`. The generated default is `true`, and while config.toml only drives the local stack, `supabase config push` applies the same file to the hosted project, which would silently reopen public signup and undo the dashboard hardening. Project ref is `jadhbtcrejkhhplhxbee`; the CLI is **not** authenticated yet (`supabase projects list` returns `LegacyPlatformAuthRequiredError`), so `login`/`link`/`db push` remain the owner's to run.
- The sign-in flow is a **magic link, not a typed OTP code**, and the distinction matters for dashboard setup. `sign-in-form.tsx` calls `signInWithOtp({ shouldCreateUser: false, emailRedirectTo: origin + "/auth/callback" })`, and `auth/callback/route.ts` reads a `?code=` param and calls `exchangeCodeForSession` — the PKCE flow `@supabase/ssr`'s `createBrowserClient` uses by default. There is no field anywhere in the UI to enter a 6-digit code. So the Magic Link email template must keep `{{ .ConfirmationURL }}`; switching it to `{{ .Token }}` would send a code with nowhere to type it. Supabase has no "passwordless" toggle — passwordless is just the Email provider with the password path unused.
- Two dashboard steps that are easy to miss and both fail confusingly: **Authentication → URL Configuration** must carry the deploy origin as Site URL plus `<origin>/auth/callback` in the redirect allowlist, or the email arrives and the link dead-ends; and the **`owner_allowlist` insert must come before the invite**, not after. `pocket_chief_owner_only_signup` is a `before insert on auth.users` trigger (202608120001) that raises `Pocket Chief signup is restricted to its configured owner` for any email not already in `public.owner_allowlist`, so inviting first fails at the dashboard with that message. Correct order: insert the allowlist row → invite the owner → test sign-in. Testing before the user exists also fails, with the same generic "Sign-in was not accepted" message a non-owner rejection produces, because `shouldCreateUser: false`. Disabling public signup does not block the owner — signup governs account creation, not authentication of an existing user.
- 2026-08-15 hosting decided: **Vercel Hobby** (free tier; personal, non-commercial, single-user). GitHub Pages was considered and rejected for two independent reasons — the app is not static (18 route handlers, 9 `force-dynamic` pages, and `proxy.ts` running the auth gate on every request; `output: "export"` rejects all three), and more decisively **GitHub Pages sites are public even when published from a private repo** (private Pages is Enterprise Cloud only), which would put licensed SCORE/Fiser-derived content on the open internet and would not even carry the `noindex` headers, since those need a server. Any static host is ruled out by the same server-side auth gate. Netlify's free tier is the viable fallback if Vercel is ever unwanted — `netlify-cli` is already wired for the AIS site. Sign up to Vercel **with GitHub** (it needs the repo connection anyway) and scope the GitHub App to **only** `pocket-chief`, not all repositories, since the vault holds other private work.
- 2026-08-15 Supabase project is live and smoke-tested. Migrations applied to `jadhbtcrejkhhplhxbee`; all tables exist and RLS blocks anonymous reads — `topics`, `taxonomy_nodes`, `topic_versions`, `sources` and `owner_allowlist` all return `[]` to the browser key, `owner_allowlist` included even though it has a row, which confirms the deliberate RLS-on/zero-policies lockdown. Auth settings verified externally via `/auth/v1/settings`: email provider on, `disable_signup: true`. A production build against the real project succeeded and every private route (`/`, `/topics`, `/saved`, `/settings`) plus every API route including `/api/library/install` returned 307 to `/auth/sign-in?next=…` when signed out, with no bounce to `/configuration-error`. The signed-in path (owner session → `ensure_launch_topic` → `installLibrary`) is still unverified because it needs a magic-link click; do that on the deployed site.
- The project uses the **new-format publishable key** (`sb_publishable_…`), not the legacy `anon` JWT. Confirmed working against supabase-js 2.112.3. Both key formats exist on the project; `supabase projects api-keys --project-ref <ref>` lists all four. Never wire `service_role` or `sb_secret_` into this app — nothing uses them and they bypass every RLS policy.
- **Config beats the demo flag, everywhere.** `resolveDataMode()` checks `hasConfig` before `POCKET_CHIEF_DEMO`, so the presence of the `NEXT_PUBLIC_SUPABASE_*` values silently wins. Restoring local demo mode means commenting the Supabase values out of `.env.local`, not just setting the flag; `.env.local` carries both, with the Supabase pair commented and instructions inline. The proxy used to check the demo flag FIRST and disagree — with both set in development it waved requests through unauthenticated while the repository still resolved to Supabase, and the symptom was an empty library with no error and no redirect, indistinguishable from content failing to register. Fixed 2026-08-15: the resolver moved to `src/lib/data-mode.ts` (import-free so the proxy can share it without dragging `next/headers` into that bundle), `src/lib/auth.ts` re-exports it, and `src/proxy.ts` calls `dataMode()` instead of recomputing. The owner email is now part of `hasConfig` too, so a half-configured deploy resolves `misconfigured` and gets `/configuration-error` rather than an unreachable sign-in loop. `auth-mode.test.ts` covers the both-set case at both layers — that duplication is what allowed the drift, so keep the single resolver.
- 2026-08-16: the app was already deployed and nobody knew. Vercel project `pocket-chief` (`prj_yYuvG9BsIEnNDwx2U5pYbZQJAV9c`, team `zachpnw-7238's projects` / `team_ChoowkA2yvlhXMxSsJoyUEs9`), production alias **https://pocket-chief-nine.vercel.app**, built from `1f7fc1b`. Hosting decision from 2026-08-15 is therefore executed, not pending.
- **"Locked" was three stacked causes, not one.** (1) Vercel turns on **SSO protection by default** for new projects — `ssoProtection: { enabled: true, deploymentType: "all_except_custom_domains" }` — which walls every `*.vercel.app` alias behind a Vercel login. It is invisible from inside the app and invisible to the project owner in a browser already signed in to Vercel, which is exactly why it read as "works on my Mac, locked on my phone". Disabled 2026-08-16 via the Vercel MCP; safe because `proxy.ts` and RLS are the real gates and `noindex` is set in `next.config.ts`. (2) Underneath it, **no environment variables were ever set on the Vercel project** — importing the GitHub repo does not carry `.env.local` — so `hasPrivateConfig()` is false, `dataMode()` returns `misconfigured`, and every route redirects to `/configuration-error`, whose headline is literally "Pocket Chief is locked". That page's wording is what the owner actually saw. (3) The owner has never completed a sign-in: `auth.users` holds `zachpnw@gmail.com` with `email_confirmed_at` null, and `topics` is 0 because nothing has ever made an authenticated request to trigger `ensure_launch_topic` or `installLibrary`.
- **Do not infer Vercel env state from `demo: false` or from bare bundle greps.** Two false positives cost real time here. `isDemoMode()` is false for *both* `supabase` and `misconfigured`, so a rendered sign-in page proves nothing about configuration. And grepping the client chunk for `supabase.co` or `sb_publishable_` always hits — they are `@supabase/supabase-js` internals (a `"*.supabase.co"` host allowlist and a `e.startsWith("sb_publishable_")` predicate). The only sound test is grepping for the *project-specific* literal, `https://<ref>.supabase.co`; `NEXT_PUBLIC_*` values are inlined at build time, so their absence there means they were absent from the build.
- 2026-08-16 **Pocket Chief is live with all 17 topics.** Owner signed in, `installLibrary` reported 17 installed, and Postgres holds 17 topics / 17 approved versions / 7 sources / 13 taxonomy nodes. The launch sequence is finished; what remains is the phone install and polish.
- **`ensure_launch_topic` could never have worked against hosted Supabase.** It failed with `function digest(bytea, unknown) does not exist` on every authenticated request. `digest()` is pgcrypto, Supabase installs extensions into the `extensions` schema, and the function is `SECURITY DEFINER set search_path = public` — so the bare `digest(...)` in the release-digest check was unresolvable. Fixed by `20260816205528_launch_topic_search_path.sql`, which recreates the function with `set search_path = public, extensions` (202608130001 is applied and immutable). This is the archetype of the risk already recorded here: it is the one path that only executes against a real project with a signed-in owner, which was the single step never verified before deploy. **Any new SECURITY DEFINER function that calls a pgcrypto/uuid-ossp helper needs `extensions` on its search_path.**
- The seed failing is **not fatal** — `SupabaseRepository.create()` catches it, logs, and resets `launchTopicSeedAttempt` so it retries. And `installLibrary` never touches `digest`: it replays all 17 topics through `createTopicDraft` → `approveDraft`. So `POST /api/library/install` populates the whole library even while the pinned seed is broken, which is how the library landed before the migration was written.
- The pinned launch digest now lives in two migrations, because immutability forces each recreation to copy it forward. `content-contract.test.ts` used to read `202608130001` by name, which would have let a stale pin in the newest — the live one — pass unnoticed; it now scans every migration and asserts all pins match.
- `vitest.config.ts` excluded `tests/e2e/**` relative to the repo root, so a leftover git worktree under `.claude/worktrees/` ran every unit test a second time and surfaced a Playwright spec vitest cannot execute — presenting as "1 failed test file, 190 tests passed" when the real suite is 95. Globs are now anchored (`**/tests/e2e/**`, `**/.claude/**`). A stale worktree for the merged `claude/sweet-haibt-314cc2` branch is still checked out there.
- 2026-08-16 the PWA had **never cached anything**. `sw.js` called `response.clone()` inside the `caches.open(...)` callback, which resolves a microtask after `return response` has handed the body to the page — so every cacheable GET threw `Failed to execute 'clone' on 'Response': Response body is already used` (96 console errors in one session) and no `cache.put` ever ran. The clone must be taken synchronously, with `event.waitUntil` holding the worker open for the write. Caches were also bumped to `v2`: the shell precache uses `cache.addAll` on `/`, `/topics`, `/saved`, and `fetch` follows redirects, so any install during the unconfigured or signed-out eras stored the "Pocket Chief is locked" or sign-in page under those keys — exactly what an offline launch would serve. The `activate` handler deletes any `pocket-chief-*` cache outside the current pair, so renaming is the purge. The bump was not precautionary — `AppShell` skips its frame only for `/auth/*`, so `/configuration-error` renders the full shell *including* `ServiceWorkerRegistration`. The worker therefore installed while the deployment was unconfigured and precached the "locked" page under `/`, `/topics` and `/saved`. Verified after deploy: `pocket-chief-content-v2` populates and the console is clean. Note the corollary for future debugging — the worker never registers on `/auth/*` at all, so the sign-in page cannot be used to test it.
- Vercel binds environment variables when a deployment is **created**, so adding them to the dashboard never fixes an existing deployment — a redeploy is always required. The Vercel MCP exposes deployment-protection but **no environment-variable tool**, and the CLI needs an interactive login, so env vars stay an owner-performed dashboard step.
- 2026-08-17 the reading view was rebuilt to read like AMBOSS: bolded key terms, underlined cross-links, three-level bullet nesting, pearl/mnemonic/danger callouts, collapsible sections, and an outline that tracks scroll position. The problem it solved was a **hierarchy inversion** — body copy rendered at 13px in `--ink-muted` while headings were 15px in `--ink`, so body text was both smaller *and* lower-contrast than headings barely larger than it. Now h2 is 17px/700 (16px on mobile) against 14px body, both at full `--ink`. Design tokens for type, spacing and radii were introduced at the same time; none existed before, and every dimension was a hardcoded literal.
- **Inline markup lives inside the plain content strings, not in the data model, and that was the whole design.** `**bold**`, `[[links]]`, a leading `- `/`-- ` for bullet depth, and a leading `↳ ` for subsection headings. Four layers validate block content and markers are invisible to all of them: `factualUnits()` reads the strings verbatim, `sourced()` *derives* claims from `factualUnits()` so `claims[i].text` picks the markers up automatically, the SQL `topic_block_expected_claims` derives from the same jsonb, and `normalize()` in search.ts strips non-alphanumerics so `**Borchardt**` still indexes as `borchardt`. The SQL is what ruled out the obvious alternatives: `bullets` uses `jsonb_array_elements_text(block->'items')`, which hard-requires an array of plain strings, so nesting the *data* would break approval; and an unrecognized block `type` falls through to `else '{}'::text[]` (zero expected claims), so new `pearl`/`mnemonic` block types would break approval too. Hence indent markers rather than nested data, and an optional `tone` field rather than new block types. **One hard rule: markers must wrap whole words** — `chole**docho**lithiasis` normalizes to three broken tokens and would break search. `content-contract.test.ts` now enforces marker balance, whole-word wrapping, and that `stripMarkup` leaves nothing behind on any Anki export path.
- Adding an optional field to a block type requires editing **`src/lib/schemas.ts` as well as `src/lib/types.ts`** — zod strips unknown keys by default, so a field present in the TS type but absent from `topicBlockSchema` is silently dropped whenever a block passes through `reviseBlockSchema.parse()` in the draft-revise route or the AI adapter. `tone` was the first field to hit this.
- **Do not rename a topic to an outline title unless the content actually covers that title's scope.** "Acute Liver Failure" was renamed to the outline's "Hepatic Failure and Hepatorenal Syndrome" to make coverage countable, then reverted: all ten blocks are ALF, with no HRS definition, criteria, staging or therapy. Matching the label without matching the content makes coverage look complete when it is half done, and a rushed lookup would find a title promising something the topic does not hold. The outline now records that section as partial. Renaming also changes the slug, and an old `/topics/<old-slug>` URL renders a blank "Topic" shell with a 200 rather than a clean 404 — worth fixing if renames become common.
- Two features from the AMBOSS reference were deliberately **dropped**: superscript citation markers, because nearly every topic cites exactly one source and the page would fill with identical `[1]` markers (the data is there — `claims[i].citationIds` is index-aligned with `factualUnits(block)[i]` — so this is revivable once topics routinely cite several works); and the bottom reading toolbar, because it collided with the existing fixed mobile nav and would have cost one-tap access to Search and Saved while reading.
- `src/content/topics/choledocholithiasis.ts` is the **one topic with no inline markup**, because its blocks are sha256-pinned by `ensure_launch_topic`. Migrating it means a new timestamped migration carrying a new digest plus the constant in `content-contract.test.ts`. Left as-is deliberately.
- A **project-level `.claude/launch.json`** appeared inside `Pocket Chief/` (untracked) with a `pnpm run dev` entry on port 3000. It shadows the vault-level config, and `pnpm` is not on PATH, so `preview_start` failed with "Command not found: pnpm". Rewritten to `node node_modules/next/dist/bin/next dev` on port 3210 with `autoPort`. Port 3000 also collides with the server Playwright starts for itself.
- The redesign made two Playwright tests fail on the bookmark button, and the cause was **test isolation, not app code**. Mechanism, confirmed by bisection: Playwright's `name` option matches accessible names by **substring**, so `getByRole("button", { name: "Save" })` also matches **"Saved offline"**. The demo store lives on `globalThis`, so one test's bookmark survives into the next test even though each gets a fresh browser context — `OfflineHydrator` syncs the server's bookmarks back into the new context's IndexedDB. The second test therefore opened with the button already reading "Saved offline", the substring locator clicked it, and it **un-saved** the topic, leaving the label at "Save". Fixed with a `test.beforeEach` that clears every bookmark through `/api/bookmarks`, plus `exact: true` on both Save locators. 12/12 green.
- **Why that surfaced only after the redesign, and the lesson**: at HEAD it passed because `TopicContent`'s mount effect read IndexedDB *before* `OfflineHydrator` wrote to it. The redesign added enough client render work to the topic page (inline parsing on every factual unit, nested lists, `<details>` per section) to delay that effect past the hydrator's write and flip the race. So a passing suite was resting on render timing. When a test breaks after a change that provably does not touch its code path, suspect a latent race the change re-timed — do not assume the change is at fault, and do not assume it is innocent either. The decisive evidence was running the suite at pristine HEAD in a worktree (12/12) and then in isolation (also green), which localized it to test ordering.
- Getting a HEAD baseline needs a worktree with a **real** `node_modules` — Turbopack rejects a symlink pointing outside the project root with "Symlink [project]/node_modules is invalid". `cp -Rc` (APFS clone-on-write) copies the 600 MB tree in about 6 seconds and costs almost no disk, which makes a throwaway baseline worktree cheap. This is the reliable way to answer "was this already broken?" without `git stash` on a dirty tree.
- **Still open, owner-only**: the four Gemini-authored topics (paraesophageal hernia repair, renal artery disease, acute liver failure, cardiac pacing) have no provenance in their source `details` fields. Every earlier source records what was corrected during review and what was added beyond the packet pending sign-off; these just summarize the topic. Given the density of specific numbers in them — King's College criteria, DeMeester > 14.72, patency 74–79%, morbidity 15.0%/23.0% — nothing marks which came from the supplied packet and which the model added. Three of them also fold the personal packet *and* a citable SCORE module into a single `kind: "user_notes"` record, where the house convention keeps them as separate sources joined by `additionalSourceIds`.
- 2026-08-17 Pediatric Surgery branch opened with Malrotation and Malrotation Operation, from the `…1100` block. The split follows `score-module-outline.md` exactly, which lists **Malrotation** under Diseases/Conditions and **Malrotation Operation** under Operations/Procedures — two topics, not one, because the disease is tested on diagnosis and the operation on technique. Both SCORE modules were already on disk; only the two owner-supplied documents were new.
- The malrotation packet arrived truncated the first time, ending mid-sentence at "Etiologies of duodenal", and the full version had to be re-supplied. Worth checking a pasted or attached packet actually reaches its last section before starting — the truncated copy read as complete because section 1 was intact and nothing announced the cut.
- **The two supplied documents contradicted each other on cecopexy**, and that turned out to be the most useful thing in either. The malrotation summary said added cecopexy may reduce recurrence; the Ladd playbook said never fix the bowel routinely, because artificial fixation recreates the narrow axis the operation exists to abolish. Fiser sides with the first and writes `cecopexy` into the keyed steps. Resolved as a genuine split rather than a winner: routine fixation is out of the contemporary operation, selective cecopexy persists in redo and post-laparoscopic-recurrence settings. Recorded as a "Board answer versus current practice" block, which is now the third use of that pattern (desmoids, AAA surveillance, this).
- Editorial precedent extended again: that block type now also covers **two supplied sources disagreeing with each other**, not just a source being dated or wrong. The same topic carries a second instance — Fiser gives age at presentation as 75 percent in the first month and 90 percent by one year, SCORE gives 30/60/75 percent by one month/one year/five years. The topic prints SCORE's and explains that the two are almost certainly counting different populations (symptomatic neonates versus all diagnosed rotational anomalies), because a reader who meets both numbers elsewhere needs to know which question each answers.
- The malrotation packet carried markdown links to an external rare-disease site (`/rare-disease/gastroschisis` and three others) — artifacts of whatever produced it. Stripped. **Check supplied packets for foreign link syntax before authoring**; the app has no such routes and they would have rendered as dead links.
- Nothing in the app broke this time. This is the first batch since the library was small that exposed no single-topic assumption — search scoring, the curriculum tree, section grouping, and the flow renderer all absorbed a new top-level section and two 12- and 10-node flows without changes. Verified after rebase: tsc and eslint clean, vitest 124/124, playwright 12/12, `/api/library` at **23 topics across 13 sections**, both topics fully Supported, and the auto-linker resolving cross-links in both directions between them without a single authored `[[link]]`.
- **Auto-linking means a topic gains cross-links it never authored, including inbound ones from topics written months earlier.** Malrotation was authored with no `[[...]]` markers at all and still renders eight outbound links, because `buildLinkIndex` scans every title and alias at render time. The practical consequence for authoring: **aliases are now load-bearing for cross-linking, not just for search**. A generous alias list is what lets other topics link *into* the new one.
- **Two sessions worked this repo in parallel on 2026-08-17 and neither knew.** The malrotation branch was cut from `b415f99` and by the time it was ready `main` had moved nine commits: the AMBOSS reading view, the inline-markup primitive, the library-wide markup migration, and four Gemini-authored topics. Three collisions had to be resolved by hand on rebase, and the shape of them is the lesson — **the `…0700` source block had been claimed by acute liver failure**, taxonomy `order` had been renumbered to 19 by three new sections, and the whole library had moved onto markup this branch's two topics did not use. `git fetch` before allocating a UUID block or a taxonomy order, not just before pushing; both are append-only registries with no uniqueness check in the type system or the contract test.
- The UUID convention on `main` is now **one hundred-block per topic** (`0700` acute liver failure, `0800` cardiac pacing, `0900` paraesophageal, `1000` renal artery). Malrotation deliberately breaks it by sharing one block across two topics — `1100`–`1103` are the four sources, `1104`/`1114` and `1105`/`1115` the two topics — because both topics cite all four sources and splitting them across blocks would imply a provenance split that does not exist.
- 2026-08-18 the `/topics` page was reworked to one tier of disclosure, and the **accordion tree it replaced was rejected on purpose**. A three-level `role="tree"` with an expand/collapse-all toolbar and its own filter box was specced; at 23 topics across 8 categories that trades a one-scroll scan for two taps per topic, duplicates the shell's typo-tolerant search with a naive `includes()` filter, and repeats the same two strings ("Diseases & Conditions" / "Operations & Procedures") at 14 of 22 nodes. What shipped instead: categories are native `<details>` (free keyboard, Find-in-page expansion, and a no-JS default of everything visible, matching `.section-block` on the topic page), the level-2 nodes are plain subheadings over the existing `TopicCard` grid, and the old decorative `.curriculum-tree` became a real anchor rail — sticky at ≥760px, a horizontal chip row below it so 8 stacked links don't push every topic off the phone.
- **Collapse state has to survive a client-side `<Link>` round trip, and `history.replaceState` alone does not do it.** State lives in the URL as `?closed=<ids>` (the closed set, so the default of everything-open keeps the URL clean) written with `replaceState` — `router.replace` would re-run this `force-dynamic` page on every toggle. But going back into `/topics` is served from the App Router cache, whose payload predates the `replaceState` and has every section open again, so the param must also be **read back onto the DOM** on mount and on `popstate`. Playwright caught this; the manual browser check missed it because `navigate` does a hard load, not a client-side navigation.
- A `::details-content` height transition (`interpolate-size: allow-keywords` + `block-size: auto` + discrete `content-visibility`) was tried and **removed — it wedged Chrome's paint** over a subtree this size, leaving a blank viewport with a correct DOM behind it. The caret rotation carries the state change instead, which is also what the topic-page sections already do.
- **That rework shipped a production-only regression, and the shape of it is the durable lesson: taxonomy node ids are not the same values in both data modes.** `taxonomySections()` filtered top-level categories with `node.parentId === "score"` — the authored id from `src/content/taxonomy.ts`. In Supabase every `taxonomy_nodes.id` is a uuid (`syncTaxonomy` in `library-install.ts` reconciles the two sides **by slug**, and `ensure_default_taxonomy` seeds the root with slug `score` under a generated uuid), so the filter matched nothing and `/topics` rendered an empty rail, zero sections, and a footer reading "23 reviewed topics across 0 categories". Every one of the 130 unit tests and 16 Playwright tests passed, because **both suites run demo-only** (`POCKET_CHIEF_DEMO=true`) where the authored string ids are real. Fixed 2026-08-18 by matching the root on `slug`, not `id`. The rule going forward: **anything that names a specific taxonomy node must key on `slug`** — it is the only identifier that survives the demo store's readable ids and Supabase's uuids — and any test that pins an id is testing demo mode alone.
- The same fix closed a second, demo-reachable half of it: the Settings taxonomy editor saves a new top-level category with **no parent at all** (`parentId: null`), not parented to the SCORE root, so an owner-created category and every topic under it were invisible on `/topics` too. A category is now "child of the root, **or** parentless and not the root itself."
