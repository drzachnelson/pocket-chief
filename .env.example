# Supabase (browser-safe project identifiers)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Exact lowercase owner email. Keep server-only.
POCKET_CHIEF_OWNER_EMAIL=

# OpenAI — server-only
OPENAI_API_KEY=
POCKET_CHIEF_TOPIC_MODEL=gpt-5.6-terra
# Optional. Falls back to POCKET_CHIEF_TOPIC_MODEL when unset.
POCKET_CHIEF_CLOZE_MODEL=

# Server-only. Set true only for a local development preview without Supabase.
POCKET_CHIEF_DEMO=true
